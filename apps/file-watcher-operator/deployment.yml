apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: file-watcher-operator
  namespace: argocd
spec:
  destination:
    namespace: ir-file-watcher
    name: k0s-cluster-staging
  project: staging
  source:
    path: components/file-watcher-operator
    repoURL: https://github.com/interactivereduction/gitops.git
    targetRevision: HEAD
    directory:
      recurse: true
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
