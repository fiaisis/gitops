apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ir-api
  namespace: argocd
spec:
  destination:
    namespace: ir
    name: k0s-cluster-staging
  project: staging
  source:
    path: components/ir-api
    repoURL: https://github.com/interactivereduction/gitops.git
    targetRevision: HEAD
    directory:
      recurse: true
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
