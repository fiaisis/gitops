---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: memphis-production
  namespace: argocd
spec:
  project: prod
  source:
    repoURL: 'https://k8s.memphis.dev/charts/'
    targetRevision: 1.2.3
    chart: memphis
    helm:
      values: |
        global:
          cluster:
            enabled: true
  destination:
    namespace: memphis
    name: k0s-cluster
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
