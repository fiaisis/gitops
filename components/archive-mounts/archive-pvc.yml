---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: archive-pvc-smb
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 1000Gi
  volumeName: archive-pv-smb
  storageClassName: smb
