apiVersion: apps/v1
kind: Deployment
metadata:
  name: flower
spec:
  template:
    spec:
      containers:
        - name: flower
          env:
            - name: CELERY_BROKER_URL
              valueFrom:
                secretKeyRef:
                  name: flower
                  key: CELERY_BROKER_URL

