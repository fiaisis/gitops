apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: management-reduce-isis-tls
  namespace: landing-page
spec:
  type: kv-v2
  refreshAfter: 30s
  vaultAuthRef: static-auth

  # Things to change:
  mount: fia
  path: fia/management/management-reduce-isis-tls
  destination:
    name: management-reduce-isis-tls
    overwrite: true
    create: true
