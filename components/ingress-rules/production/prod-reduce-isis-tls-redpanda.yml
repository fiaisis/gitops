apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: prod-reduce-isis-tls
  namespace: redpanda
spec:
  type: kv-v2
  refreshAfter: 30s
  vaultAuthRef: static-auth

  # Things to change:
  mount: fia
  path: prod/prod-reduce-isis-tls
  destination:
    name: prod-reduce-isis-tls
    overwrite: true
    create: true
