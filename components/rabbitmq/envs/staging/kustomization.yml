apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

components:
#  - ../../variants/servicemonitor
  - ../../variants/llsp-vhost

patchesStrategicMerge:
  - rabbitmq-admin-secret.yml