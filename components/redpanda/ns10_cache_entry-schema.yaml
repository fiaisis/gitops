apiVersion: schemas.vectorized.io/v1alpha1
kind: Schema
metadata:
  name: ns10_cache_entry
spec:
  subject: ns10_cache_entry-value
  schemaType: flatbuffers
  schema: "\nfile_identifier \"ns10\";\n\n/// pylint: skip-file\ntable CacheEntry\
    \ {\n        key:string;            // key for this entry (usually nicos/device/parameter)\n\
    \        time:double;           // time (in seconds after epoch) when this entry\
    \ was set\n        ttl:double;            // time to live (in seconds after time\
    \ field of this entry)\n        expired:bool = false;  // already expired (manually\
    \ or using ttl), supersedes ttl\n        // Value for the key.\n        // The\
    \ value can be numerical types, strings, list, tuple, dictionaries, sets \n  \
    \      value:string;\n}\n\nroot_type CacheEntry;\n"
