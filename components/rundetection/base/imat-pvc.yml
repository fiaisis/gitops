---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: rundetection-imat-pvc
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 1000Gi
  volumeName: rundetection-imat-pv-smb
  storageClassName: ""