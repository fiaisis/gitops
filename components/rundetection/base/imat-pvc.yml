---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: plotting-service-imat-pvc
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 1000Gi
  volumeName: plotting-service-imat-pv-smb
  storageClassName: ""